# weaver2 "범용 레코드 CRUD" 방향 구체화 — 설계

- **날짜**: 2026-07-10
- **상태**: 설계 승인됨 (구현 전)
- **참여**: 주인님(방향 결정) · 노엘(설계 정리)
- **관련 문서**: [`CHARTER.md`](../../../CHARTER.md) §1 · §5.1 · §6

---

## 1. 배경 — 던져진 질문

CHARTER가 weaver2를 "범용 레코드-CRUD 기반 + 배터리 보일러플레이트"로 재정의한 뒤(2026-07-02, `ca60102`·`2adfec1`), 자연스럽게 따라온 질문:

> "어떤 테이블(엔티티)이 와도 **최소한의 코드만 추가하면** CRUD 화면과 API가 만들어지는 템플릿이어야 하는가?"

이 문장은 하나의 목표처럼 들리지만 실제로는 세 가지 서로 다른 만듦새로 갈라지며, 어느 쪽이냐에 따라 로드맵이 통째로 달라진다.

## 2. 검토한 대안

| 대안 | 내용 | 판단 |
|------|------|------|
| **런타임 제네릭 엔진** | 엔티티 메타데이터 등록만으로 API·화면이 런타임 생성 (Directus/Django admin류) | ❌ 커스터마이징 절벽, 타입 안전성·CQRS 파일 분리 정체성과 충돌, §5.1 기준 "상상 기반 일반화" |
| **스캐폴드 CLI 코드젠** | `pnpm gen:crud` 결정적 제너레이터가 템플릿에서 파일 생성 | ❌ 템플릿이 제2의 유지보수 대상 — substrate 변경마다 이중 관리 |
| **패턴 + AI 레시피** | 레시피 문서(스킬)를 따라 AI가 표준 세트를 조립. 결과물은 전부 소유하는 평범한 코드 | ✅ **채택** — CHARTER §1 "(AI와 함께) 조립" 미션·§5.1 pull-not-push에 부합, 마법 없음 |

### 레퍼런스 엔티티에 대한 결정 (가이드만으로 불충분한가?)

살아있는 레퍼런스 엔티티의 값어치(컴파일러가 드리프트 감지 / AI 복제 충실도 / 레시피의 테스트 타깃)와 비용(가짜 도메인 상주, fork 체크리스트 증가)을 비교한 결과:

- **가이드 우선.** 코드 예시는 이미 존재한다 — 게시판(백엔드 spec 제외 67파일). 가이드가 게시판의 코어/특수 경계를 정밀하게 그어주면 복제 충실도 문제는 상당 부분 해소된다.
- "AI가 가이드+게시판만으로 헤맬 것"은 아직 fork 0회인 현시점엔 **상상 기반 우려**다. §5.1 렌즈 1(증거 기반)에 따라, **리허설·실전에서 반복적으로 헤매는 증거가 나오면 그때** 미니멀 예시를 본체에 추가한다.

## 3. 확정된 결정 요약

1. **방식**: 패턴 + AI 레시피 (런타임 엔진 ❌, CLI 코드젠 ❌)
2. **화면 범위**: 관리자 화면(목록·생성·상세/수정)은 완전 표준 + 공개 화면(목록·상세)은 기본형 레퍼런스
3. **레퍼런스**: 가이드 우선 — 게시판 코어를 정밀 포인터로 지정. 미니멀 예시 추가는 증거가 끌어당길 때만
4. **코어 경계 (슬림 코어)**: CRUD 5종 API(CQRS 파일 분리) + `PERMISSIONS` 상수·가드 + soft-delete·audit 필드 + keyset 목록 + 관리자 필터·정렬. 첨부·풀텍스트 검색·알림은 옵션 add-on 섹션
5. **형태**: 프로젝트 스킬 `.agents/skills/weaver-crud-recipe/` (+ `.claude/skills` 심링크, agent-agnostic)

## 4. 산출물 설계 — `weaver-crud-recipe` 스킬

```
.agents/skills/weaver-crud-recipe/
  SKILL.md              ← 트리거, 표준 세트 정의, 코어/특수 경계선 선언
  references/
    backend.md          ← prisma model → migration → PERMISSIONS → 시드 매핑 → CQRS 4층
    admin-ui.md         ← WeaverDataTable 목록+필터·정렬 / 생성 폼 / 상세·수정
    public-ui.md        ← 공개 목록/상세 기본형
    addons.md           ← 첨부파일 / 풀텍스트 검색 / 알림 연동 (필요한 엔티티만)
```

### 4.1 SKILL.md

- **트리거**: "새 엔티티", "테이블 추가", "CRUD 추가" 류 요청
- **표준 세트 정의**: §3-4의 슬림 코어를 명문화
- **경계선 선언**: 게시판에서 무엇이 표준 골격이고 무엇이 게시판 특수(대댓글·리액션·고정·카테고리)인지 명시 — AI가 매번 추리지 않도록

### 4.2 references/*.md 공통 원칙

- 각 단계마다 **게시판 코어 파일의 정확한 포인터** 제공: "이 파일의 이 부분을 미러링하라, 이 부분은 게시판 특수이니 무시하라"
- 산문 지시보다 **구체 코드 지시** 우선 (few-shot 원리)
- 기존 컨벤션 재선언 금지 — `weaver-coding-standards`·`nestjs-best-practices` 스킬이 이미 다루는 내용은 참조만
- prisma 규칙: FK 컬럼명 `{참조테이블명}Id`, soft-delete(`deletedAt`)·audit(`createdAt`/`updatedAt`) 필드
- **테스트 포함**: 코어 레시피에 서비스 유닛 테스트 단계 포함(기존 유닛 테스트 패턴 미러링). e2e는 선택 노트

## 5. 검증 계획 — 리허설이 곧 레시피의 테스트

1. **일회성 브랜치**에서 가상 엔티티(예: `inventory`)를 **레시피만 보고** AI가 조립하는 실험
2. **성공 기준**: lint·typecheck·기존 테스트 전부 통과 + 표준 세트(백엔드 4층 + 관리자 3화면 + 공개 2화면 + 유닛 테스트) 완성 + 사람 리뷰에서 컨벤션 위반 0
3. **피드백 루프**: 헤맨 지점 → 레시피 문서 수정. 반복해서 헤매는 지점 → 그때 미니멀 예시를 본체에 추가(증거 기반). 실험 브랜치는 폐기

## 6. 문서 반영 (소폭)

- `CHARTER.md` §6 확장 포인트 표에 스킬 한 줄 추가
- `CLAUDE.md` 프로젝트 스킬 목록에 `weaver-crud-recipe` 추가
- `README.md` 사용법에 링크 한 줄
- (별건) CHARTER §7.3의 "docs/* 는 git ignore (`!docs/audits/`만 추적)" 문구가 실제 `.gitignore`(`docs/local/`만 제외, docs/ 전체 추적)와 어긋남 — 함께 정정

## 7. 의도적으로 하지 않는 것

- CLI 코드젠 도입
- 런타임 제네릭 엔진
- 게시판을 코어/확장으로 분해하는 리팩토링 (동작 중인 67파일 대수술 + 회귀 위험)
- 미니멀 레퍼런스 엔티티 선행 추가 (증거가 끌어당기면 그때 — §5.1)

## 8. 구현 순서 (다음 단계)

1. `weaver-crud-recipe` 스킬 작성 (SKILL.md + references 4종)
2. 리허설 실험 및 레시피 보정
3. 문서 반영 (CHARTER §6·§7.3, CLAUDE.md, README)

각 단계는 별도 구현 계획(writing-plans)으로 상세화한다.
