# Claude Code Assistant Guidelines

## 🎯 **핵심 철학**

**주요 목표:** 개발 및 학습을 돕고 엔지니어의 더 나은 문제 해결 능력 및 생산성 향상

### 가치 지향
- **학습 중심**: 사고 과정과 원리 이해를 돕는 교육적 접근
- **협력적 문제 해결**: 함께 탐구하고 발견하는 파트너십
- **지속 가능한 성장**: 반복 가능한 패턴과 체계적 사고력 구축
- **품질과 효율의 균형**: 빠른 개발과 코드 품질의 실용적 조화

---

## 🧠 **핵심 방법론: EDGE**

**E**xplain → **D**emonstrate → **G**uide → **E**nhance

단순 코드 제공이 아닌 학습 중심 문제 해결 지향

---

## ⚡ **필수 워크플로우 (3단계)**

```
1️⃣ 계획 보고 → 사용자 승인
2️⃣ 코드 작성 (기존 패턴 준수)
3️⃣ 완료 보고 → 커밋
```

> 로컬 커밋은 자율. 단 **push는 확인을 받는다** (되돌리기 어렵고 외부에 영향을 주므로).

### 긴급 상황 (버그 수정)
- 계획 보고 간소화 가능
- 완료 보고는 여전히 필수

---

## 🚫 **절대 금지 사항**

```markdown
❌ 커밋 메시지에 AI 생성 표시
❌ fetch() 직접 사용 (ApiClient 필수)
❌ 공통 컴포넌트 무시하고 커스텀 구현
```

---

## 🖥️ **개발 서버**

`pnpm dev` 는 앱을 고르는 **대화형 메뉴**라 자동화에서는 입력을 기다리며 멈춘다. 앱을 직접 지정해 **백그라운드로** 띄우고, 확인이 끝나면 정리한다.

```bash
pnpm dev:core   # 백엔드 :4000
pnpm dev:web    # 프론트엔드 :3000
```

---

## 🛤️ **병렬 레인 — 세션 여럿이 동시에 고칠 때**

> 🔴 **레인에서는 `pnpm lint` 를 쓰지 않는다 — `pnpm lint:check` 를 쓴다.** 루트의 `lint` 는 `eslint … --fix` 라 **검사가 아니라 수정**이고, 내가 안 건드린 파일까지 서식을 다시 쓴다. 남의 레인 변경이 내 작업 트리에 섞이고, `lane:land` 는 더럽다며 거절한다.
>
> **일을 시작하기 전에 `pnpm lane:brief` 를 돌린다.** 규칙은 이 문서가 들고 다니지만, 살아 있는 상태(런타임 레인이 비었는지·내 레인이 뒤처졌는지·어떤 포트가 주인 없는지·원격 CI 가 빨간지)는 파일에 담을 수 없다. brief 가 그걸 한 화면에 준다.

세션 수와 **도는 서버 수**는 다른 것이다. 세션마다 풀스택을 띄우면 이 맥에서 스왑이 난다. 그래서 레인을 두 종류로 나눈다.

| | 서버 | 포트 | 검증 |
|---|---|---|---|
| **편집 레인** (기본, N개) | 없음 | 없음 | `pnpm typecheck` · `pnpm lint:check` · `pnpm test:core` |
| **런타임 레인** (1벌, 저장소 루트) | Next 3000 + Nest 4000 | 2개 | 눈으로 보기 · e2e |

편집 레인은 화면이 필요해지면 **런타임 레인(저장소 루트)에 자기 브랜치를 체크아웃해서** 본다. 자기 서버를 띄우지 않는다.

`pnpm typecheck` 는 프론트에서 `next typegen` 을 먼저 돌린다. `LayoutProps` 같은 타입은 Next 가 `.next/types/` 에 **생성**하는 것이라, `next dev` 를 한 번도 안 돌린 새 레인에는 없기 때문이다.

```bash
pnpm lane:new <이름>     # 워크트리 + 의존성 + .env + prisma 클라이언트
pnpm lane:ls             # 레인·브랜치·포트 현황 (주인 없는 포트도 같이 보여준다)
pnpm lane:rm <이름>      # 커밋 안 된 변경·안 올린 커밋이 있으면 거부한다
```

### 브랜치 지형

```
편집 레인 ─┐
편집 레인 ─┼─→ dev ──(PR)──→ main
편집 레인 ─┘   통합
```

- **`dev` = 레인들이 모이는 곳.** `lane:land` 도 여기로 넣는다. 레인은 `dev` 에서 갈라진다.
- 🔴 **런타임 레인(저장소 루트)은 `dev` 를 달고 있어야 한다.** `lane:land` 는 **루트가 지금 체크아웃한 가지**로 넣기 때문이다 — 루트가 `main` 을 달고 있으면 레인이 `main` 으로 들어간다. `pnpm lane:brief` 의 「런타임」 줄에서 확인한다.
- 내보낼 때만 `dev` → `main` PR 을 연다.
- ⚠ **CI(`ci.yml`)는 2026-09-14 현재 `main` 만 본다.** `dev` 착륙분은 PR 을 열기 전까지 원격 검사를 안 받으므로, **레인에서 돌리는 검증 3종이 지금은 유일한 관문**이다.

### e2e 는 격리 레인에서 — 줄 서지 않는다

**화면 보기와 e2e 는 다른 일이다.** 화면은 사람이 한 번에 하나만 보니 런타임 레인 순번제가 맞다. e2e 는 기계가 돌리므로 격리하면 여럿이 동시에 돈다.

```bash
pnpm lane:new <이름> --isolated    # 자기 DB + 자기 포트
pnpm lane:e2e <이름> [스펙…]        # 켜고 · 재고 · 반드시 내린다
```

- **자기 DB** — 공용을 통째로 복사한다. 그래서 남의 e2e 가 내 데이터를 안 밟고, **`pnpm db:seed` 를 마음껏 돌려도 된다.** 「전체 시드 금지」는 공용 DB 를 쓰는 레인에만 해당한다
- **`lane:e2e` 가 생애 전체를 책임진다** — 빌드(낡았을 때만) → 서버 켜기 → playwright → **trap 으로 반드시 내리기**
- **서버를 손으로 띄우지 않는다.** 켜 둔 채 잊는 것이 RAM 을 먹는다 — 레인 시스템을 만든 이유가 그것이다
- `lane:rm` 이 워크트리와 함께 **그 DB 도 지운다**

### 화면 확인과 착륙 (런타임 레인에서만)

```bash
pnpm lane:show <이름>      # 그 레인 코드를 3000 에 올린다 (git checkout --detach)
pnpm lane:back             # 제자리로
pnpm lane:land <이름>      # fast-forward 로 넣는다 (--rm 을 붙이면 레인까지 정리)
```

**순서는 「rebase 먼저, 그다음 확인, 그다음 머지」다.** rebase 를 먼저 해야 「레인에서 본 트리」와 「`dev` 에 들어갈 트리」가 같아진다(fast-forward). `lane:land` 는 rebase 가 안 됐으면 거절해 이 순서를 강제한다.

**`lane:back` 은 보는 동안 만든 커밋**이 있으면 멈춘다. detached HEAD 에서 한 커밋은 어떤 브랜치에도 안 붙어, 그냥 돌아가면 조용히 미아가 되기 때문이다.

**포트 대역은 3010~3019 · 4010~4019 다.** 3001~3009 를 쓰지 않는 이유는 이 맥에 `vands-crm-v1` 이 함께 살고 그쪽 레인 대역이 거기이기 때문이다 (겹치면 우리 e2e 가 남의 서버에 막혀 선다). 그래서 `apps/core-backend/.env` 의 `ALLOWED_ORIGINS` 에 **3010~3019 를 미리 한 번** 열어 두었다 — 프록시를 거쳐도 브라우저의 `Origin` 은 백엔드까지 그대로 가므로, 목록에 없으면 그 레인은 로그인부터 막히고 화면에는 「서버에 연결하지 못했습니다」로만 보인다.

### 레인에서 하지 말 것

- 🔴 **DB·백엔드가 공용이다 — `pnpm db:seed`(전체 시드)를 돌리지 않는다.** 옆 레인의 권한이 지워진다.
- 🔴 **prisma 스키마를 바꾸는 레인은 `--isolated` 로 만든다.** `--with-backend` 는 **포트만 가르고 `DATABASE_URL` 은 공용 그대로**라, 그 레인에서 마이그레이션을 돌리면 **공용 개발 DB 가 바뀌고 다른 레인이 전부 그 스키마를 문다.** 각자 `pnpm db:generate` 를 다시 하기 전까지 새 컬럼이 응답에서 조용히 빠진다 — 검사도 통과한다.
- 🟡 **레인이 `dev` 로 rebase 해서 새 마이그레이션이 따라왔다면** 그 레인에서 `pnpm db:generate` 를 다시 돌린다. 클라이언트는 워크트리마다 따로다. 공용 DB 에 아직 안 올라간 마이그레이션이면 누군가 한 번 **`pnpm db:deploy`** 를 돌려야 한다 — `pnpm lane:brief` 가 안 올라간 개수를 알려 준다.

> 이 체계는 `vands-crm-v1` 에서 옮겨왔다 (2026-09-14). `scripts/lane.sh` 주석 속 「실측」·날짜가 붙은 실패 기록은 **그 저장소에서 잰 값**이다.

---

## 📚 **스킬 참조**

### 프로젝트 스킬 (자동 적용)
- **`weaver-coding-standards`** - ApiClient, NestJS 패턴, 코딩 스타일
- **`weaver-ui-patterns`** - TabComponent, WeaverDataTable, URL 상태 관리
- **`nestjs-best-practices`** - NestJS 베스트 프랙티스 (40개 규칙)
- **`weaver-crud-recipe`** - 새 엔티티 CRUD 표준 세트 조립 레시피 (골격)

수동 호출 (필요시):
```
/weaver-coding        # 코딩 표준 확인
/weaver-ui           # UI 패턴 확인
/nestjs-best-practices  # NestJS 베스트 프랙티스
/weaver-crud-recipe  # 새 엔티티 얹기 레시피
```

---

## 📊 **우선순위**

**🔴 CRITICAL (필수)**
- 계획 보고/승인
- ApiClient 사용
- 공통 컴포넌트 우선
- ESLint/TypeScript 준수
- FK 컬럼명: `{참조테이블명}Id` (예: `userId`, `permissionGroupId`)

**🟡 HIGH (강력 권장)**
- EDGE 방법론
- URL 상태 관리
- 에러 처리 표준화

**🟢 MEDIUM (권장)**
- 성능 최적화
- 아키텍처 개선

---

## 🤝 **상호작용 원칙**

### ✅ 지향점
- 사고를 유도하는 질문으로 시작
- 여러 접근법과 트레이드오프 제시
- 문맥 파악 후 맞춤형 조언

### ❌ 피할 점
- 설명 없이 완성된 솔루션 제공
- 문제 파악 전 코드부터 제시
- 상황에 맞지 않는 일반론

---

## ⚠️ **예외 상황**

- **긴급 버그**: 계획 보고 간소화 가능, 완료 보고는 여전히 필수
- **실험적 기능**: 별도 브랜치, 상세 문서화, 롤백 계획
- **외부 라이브러리**: 제약사항 사전 고지, 대안 제시
- **레거시 코드**: 기존 동작 보존, 점진적 개선

---

## 📝 **프로젝트 구조**

```
weaver2/
├── CLAUDE.md                    # 이 파일 (핵심 철학 & 워크플로우)
├── .agents/skills/              # 프로젝트 스킬 중앙 저장소 (실제 파일)
│   ├── weaver-coding-standards/
│   ├── weaver-ui-patterns/
│   ├── nestjs-best-practices/
│   └── weaver-crud-recipe/
└── .claude/skills/              # Claude Code 링크 (자동 로드)
    └── [symlinks to .agents/skills/]
```

> 개인 글로벌 스킬(예: 보고 템플릿)은 각자 홈 디렉토리의 스킬 저장소(`~/.claude/skills/` 등)에 두는 선택 사항이며, 이 저장소는 프로젝트 스킬만 전제한다.

---

**참고**:
- 이 파일은 핵심 철학과 워크플로우만 포함
- 설계·계획 문서 위치: `docs/design/` (`research/` 재료 → `specs/` 설계 → `plans/` 구현 계획, CHARTER §7.3) — 도구 기본 경로 대신 이 경로 사용
- 상세 규칙은 `.agents/skills/`의 Skills에서 자동 적용
- Skills는 컨텍스트 압박 시에도 설명이 유지되어 더 일관된 품질 보장
